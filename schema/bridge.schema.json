{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://simpleguitar.app/schema/bridge.schema.json",
  "title": "BridgeMessage",
  "description": "Simple Guitar C++ <-> React bridge protocol, v3. This schema is the single source of truth for the message layer: any new UI<->engine capability starts with a change here, and TypeScript types are generated from it (schema/gen-types.mjs -> schema/gen/ts/bridge.ts). Never hand-write the TS types.\n\nUnion of every message that can cross the bridge in either direction, discriminated by the `type` field.",
  "oneOf": [
    {
      "$ref": "#/$defs/UiToEngineMessage"
    },
    {
      "$ref": "#/$defs/EngineToUiMessage"
    }
  ],
  "$defs": {
    "SchemaVersion": {
      "title": "SchemaVersion",
      "description": "Bridge protocol version. Bump this whenever a breaking change is made to any message shape in this file; engine and UI both check it on stateChanged so a stale webview bundle fails loudly instead of silently desyncing. M2 wave 2 bumps 2 -> 3 for the preset message set (loadPreset/savePresetAs/saveCurrentPreset/nextPreset/prevPreset/requestPresets/presetsState/presetResult).",
      "type": "integer",
      "minimum": 0,
      "const": 3
    },
    "ParamId": {
      "title": "ParamId",
      "description": "Identifiers for host-automatable parameters exposed by the engine's APVTS. M1 adds the gate/amp/cab rig params; M2 adds the three floor pedals (screamer/echoes/chamber). New params are added here first.",
      "type": "string",
      "enum": [
        "outputGain",
        "gateOn",
        "gateThresholdDb",
        "ampInputDb",
        "ampBassDb",
        "ampMidDb",
        "ampTrebleDb",
        "ampPresenceDb",
        "namNormalize",
        "cabOn",
        "cabLowCutHz",
        "cabHighCutHz",
        "screamerOn",
        "screamerDrive",
        "screamerTone",
        "screamerLevel",
        "echoesOn",
        "echoesTime",
        "echoesFeedback",
        "echoesMix",
        "chamberOn",
        "chamberDecay",
        "chamberTone",
        "chamberMix"
      ]
    },
    "PedalId": {
      "title": "PedalId",
      "description": "Identifiers for the three floor pedals, used only for chain ordering (each pedal's own on/off + knob params are ParamIds above). Order matters: index 0 is first in the signal path (closest to the input trim/gate), index 2 feeds the NAM amp.",
      "type": "string",
      "enum": [
        "screamer",
        "echoes",
        "chamber"
      ]
    },
    "ChainOrder": {
      "title": "ChainOrder",
      "description": "The floor pedals' left-to-right / signal order: a permutation of all three PedalIds, no duplicates, no omissions.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/PedalId"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "NormalizedParamValue": {
      "title": "NormalizedParamValue",
      "description": "A parameter value normalized to the JUCE APVTS convention of 0..1, regardless of the parameter's real-world range/units (e.g. outputGain's underlying range is -60..+12 dB). The engine owns the mapping in both directions.",
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "DecibelValue": {
      "title": "DecibelValue",
      "description": "A signal level expressed in dBFS, as reported by a meter tap. Not clamped in the schema since engines may report -inf for digital silence; consumers should treat very negative / non-finite values as silence.",
      "type": "number"
    },
    "SetParam": {
      "title": "SetParam",
      "description": "UI -> engine. Set a parameter to a new normalized value, e.g. from a knob drag. The engine is the source of truth for the resulting real-world value and will echo it back via stateChanged.",
      "type": "object",
      "properties": {
        "type": {
          "const": "setParam"
        },
        "paramId": {
          "$ref": "#/$defs/ParamId"
        },
        "value": {
          "$ref": "#/$defs/NormalizedParamValue"
        }
      },
      "required": [
        "type",
        "paramId",
        "value"
      ],
      "additionalProperties": false
    },
    "LoadNamModel": {
      "title": "LoadNamModel",
      "description": "UI -> engine. Load a .nam model file. path must be an absolute path inside the managed Models library folder; the engine rejects anything else. The engine responds with a loadResult (kind: \"nam\") followed by a fresh rigState once the load completes (success or failure).",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadNamModel"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "additionalProperties": false
    },
    "LoadIr": {
      "title": "LoadIr",
      "description": "UI -> engine. Load a cab IR file (.wav/.aiff). path must be an absolute path inside the managed IRs library folder; the engine rejects anything else. The engine responds with a loadResult (kind: \"ir\") followed by a fresh rigState once the load completes (success or failure).",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadIr"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "additionalProperties": false
    },
    "RequestRigState": {
      "title": "RequestRigState",
      "description": "UI -> engine. Ask for a fresh rigState snapshot, e.g. when a library/overlay view mounts. The engine rescans the Models/IRs library folders before replying.",
      "type": "object",
      "properties": {
        "type": {
          "const": "requestRigState"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "SetChainOrder": {
      "title": "SetChainOrder",
      "description": "UI -> engine. Commits a new floor pedal signal order, e.g. after a drag-reorder or keyboard swap gesture. order must be exactly a permutation of the three PedalIds; the engine rejects (ignores) anything else and does not echo a rigState back for a rejected message. On success the engine applies the new order glitch-free (no audio dropout) and the new order round-trips back on the next rigState (including on a later requestRigState, and after DAW state restore).",
      "type": "object",
      "properties": {
        "type": {
          "const": "setChainOrder"
        },
        "order": {
          "$ref": "#/$defs/ChainOrder"
        }
      },
      "required": [
        "type",
        "order"
      ],
      "additionalProperties": false
    },
    "LoadPreset": {
      "title": "LoadPreset",
      "description": "UI -> engine. Load a .sgpreset file. path must be an absolute path inside the managed Presets library folder; the engine rejects anything else. Restores model/IR/chain order/params via the same code path as a DAW session restore. The engine responds with a presetResult followed by a fresh presetsState once the load completes (success or failure).",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadPreset"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "additionalProperties": false
    },
    "SavePresetAs": {
      "title": "SavePresetAs",
      "description": "UI -> engine. Saves the current rig state as a new (or overwritten) preset named `name` and makes it the current preset. The engine sanitizes `name` into a safe filename; an empty/unsanitizable name is rejected. The engine responds with a presetResult (ok:false + a message on failure) followed by a fresh presetsState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "savePresetAs"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "name"
      ],
      "additionalProperties": false
    },
    "SaveCurrentPreset": {
      "title": "SaveCurrentPreset",
      "description": "UI -> engine. Overwrites the current preset with the current rig state. Fails (presetResult ok:false) if there is no current preset -- the UI should fall back to savePresetAs in that case. The engine responds with a presetResult followed by a fresh presetsState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "saveCurrentPreset"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "NextPreset": {
      "title": "NextPreset",
      "description": "UI -> engine. Loads the next preset (sorted by name) relative to the current one, wrapping around; loads the first preset if none is current. No-op if there are no presets. The engine responds with a fresh presetsState (no presetResult -- the UI has no inline failure surface for this control).",
      "type": "object",
      "properties": {
        "type": {
          "const": "nextPreset"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "PrevPreset": {
      "title": "PrevPreset",
      "description": "UI -> engine. Loads the previous preset (sorted by name) relative to the current one, wrapping around; loads the last preset if none is current. No-op if there are no presets. The engine responds with a fresh presetsState (no presetResult).",
      "type": "object",
      "properties": {
        "type": {
          "const": "prevPreset"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "RequestPresets": {
      "title": "RequestPresets",
      "description": "UI -> engine. Ask for a fresh presetsState snapshot, e.g. when the presets overlay mounts. The engine rescans the Presets folder before replying.",
      "type": "object",
      "properties": {
        "type": {
          "const": "requestPresets"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "UiToEngineMessage": {
      "title": "UiToEngineMessage",
      "description": "Union of every message the React UI may send to the C++ engine.",
      "oneOf": [
        {
          "$ref": "#/$defs/SetParam"
        },
        {
          "$ref": "#/$defs/LoadNamModel"
        },
        {
          "$ref": "#/$defs/LoadIr"
        },
        {
          "$ref": "#/$defs/RequestRigState"
        },
        {
          "$ref": "#/$defs/SetChainOrder"
        },
        {
          "$ref": "#/$defs/LoadPreset"
        },
        {
          "$ref": "#/$defs/SavePresetAs"
        },
        {
          "$ref": "#/$defs/SaveCurrentPreset"
        },
        {
          "$ref": "#/$defs/NextPreset"
        },
        {
          "$ref": "#/$defs/PrevPreset"
        },
        {
          "$ref": "#/$defs/RequestPresets"
        }
      ]
    },
    "MeterFrame": {
      "title": "MeterFrame",
      "description": "Engine -> UI. A single coalesced metering sample, pushed on a 30-60Hz timer off the audio thread (never per-block). M0 carries just the passthrough in/out peaks; more taps (per-block, tuner, etc.) are added as new fields in later milestones, not new message types.",
      "type": "object",
      "properties": {
        "type": {
          "const": "meterFrame"
        },
        "inPeakDb": {
          "$ref": "#/$defs/DecibelValue"
        },
        "outPeakDb": {
          "$ref": "#/$defs/DecibelValue"
        }
      },
      "required": [
        "type",
        "inPeakDb",
        "outPeakDb"
      ],
      "additionalProperties": false
    },
    "ParamValueMap": {
      "title": "ParamValueMap",
      "description": "Map of ParamId -> current normalized value, as sent in a full-state snapshot.",
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/ParamId"
      },
      "additionalProperties": {
        "$ref": "#/$defs/NormalizedParamValue"
      }
    },
    "StateChanged": {
      "title": "StateChanged",
      "description": "Engine -> UI. Full-chain parameter snapshot, sent on connect and whenever the engine-side state changes for a reason the UI didn't itself just cause (host automation, preset load, etc). Carries schemaVersion so the UI can detect a protocol mismatch against a stale bundle.",
      "type": "object",
      "properties": {
        "type": {
          "const": "stateChanged"
        },
        "schemaVersion": {
          "$ref": "#/$defs/SchemaVersion"
        },
        "params": {
          "$ref": "#/$defs/ParamValueMap"
        }
      },
      "required": [
        "type",
        "schemaVersion",
        "params"
      ],
      "additionalProperties": false
    },
    "LibraryEntry": {
      "title": "LibraryEntry",
      "description": "One file in the managed Models or IRs library folder.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "path"
      ],
      "additionalProperties": false
    },
    "RigLibrary": {
      "title": "RigLibrary",
      "description": "Contents of the managed library folders (Documents/Simple Guitar/Models, .../IRs), rescanned each time a rigState is produced.",
      "type": "object",
      "properties": {
        "models": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LibraryEntry"
          }
        },
        "irs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LibraryEntry"
          }
        }
      },
      "required": [
        "models",
        "irs"
      ],
      "additionalProperties": false
    },
    "RigState": {
      "title": "RigState",
      "description": "Engine -> UI. Full rig snapshot: currently loaded NAM model / IR (if any), the floor pedal chain order, and the managed library contents. Sent on page load, after any loadNamModel/loadIr/setChainOrder completes, and in reply to requestRigState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "rigState"
        },
        "schemaVersion": {
          "$ref": "#/$defs/SchemaVersion"
        },
        "namModelName": {
          "type": [
            "string",
            "null"
          ]
        },
        "namModelSampleRate": {
          "type": "number"
        },
        "irName": {
          "type": [
            "string",
            "null"
          ]
        },
        "chainOrder": {
          "$ref": "#/$defs/ChainOrder"
        },
        "library": {
          "$ref": "#/$defs/RigLibrary"
        }
      },
      "required": [
        "type",
        "schemaVersion",
        "namModelName",
        "namModelSampleRate",
        "irName",
        "chainOrder",
        "library"
      ],
      "additionalProperties": false
    },
    "LoadResultKind": {
      "title": "LoadResultKind",
      "description": "Which loader a loadResult reports on.",
      "type": "string",
      "enum": [
        "nam",
        "ir"
      ]
    },
    "LoadResult": {
      "title": "LoadResult",
      "description": "Engine -> UI. Outcome of a loadNamModel or loadIr request. message is a human-readable success/failure description (e.g. the resolved model name, or an error). Always followed by a rigState with the up-to-date snapshot.",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadResult"
        },
        "kind": {
          "$ref": "#/$defs/LoadResultKind"
        },
        "ok": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "kind",
        "ok",
        "message"
      ],
      "additionalProperties": false
    },
    "PresetsState": {
      "title": "PresetsState",
      "description": "Engine -> UI. Full preset snapshot: the current preset (if any), whether the rig has changed since the last save/load, and the managed Presets library contents (flat, sorted by name). Sent on page load, after any loadPreset/savePresetAs/saveCurrentPreset/nextPreset/prevPreset resolves, in reply to requestPresets, and whenever `dirty` flips (throttled to ~4 emissions/second max so a fast knob drag doesn't flood the bridge).",
      "type": "object",
      "properties": {
        "type": {
          "const": "presetsState"
        },
        "schemaVersion": {
          "$ref": "#/$defs/SchemaVersion"
        },
        "current": {
          "oneOf": [
            {
              "$ref": "#/$defs/LibraryEntry"
            },
            {
              "type": "null"
            }
          ]
        },
        "dirty": {
          "type": "boolean"
        },
        "presets": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LibraryEntry"
          }
        }
      },
      "required": [
        "type",
        "schemaVersion",
        "current",
        "dirty",
        "presets"
      ],
      "additionalProperties": false
    },
    "PresetResult": {
      "title": "PresetResult",
      "description": "Engine -> UI. Outcome of a loadPreset, savePresetAs, or saveCurrentPreset request (mirrors LoadResult's nam/ir pairing with rigState). message is human-readable (e.g. an error like \"name required\", or a short confirmation). Always followed by a fresh presetsState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "presetResult"
        },
        "ok": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "ok",
        "message"
      ],
      "additionalProperties": false
    },
    "EngineToUiMessage": {
      "title": "EngineToUiMessage",
      "description": "Union of every message the C++ engine may send to the React UI.",
      "oneOf": [
        {
          "$ref": "#/$defs/MeterFrame"
        },
        {
          "$ref": "#/$defs/StateChanged"
        },
        {
          "$ref": "#/$defs/RigState"
        },
        {
          "$ref": "#/$defs/LoadResult"
        },
        {
          "$ref": "#/$defs/PresetsState"
        },
        {
          "$ref": "#/$defs/PresetResult"
        }
      ]
    }
  }
}
