{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://simpleguitar.app/schema/bridge.schema.json",
  "title": "BridgeMessage",
  "description": "Simple Guitar C++ <-> React bridge protocol, v4. This schema is the single source of truth for the message layer: any new UI<->engine capability starts with a change here, and TypeScript types are generated from it (schema/gen-types.mjs -> schema/gen/ts/bridge.ts). Never hand-write the TS types.\n\nUnion of every message that can cross the bridge in either direction, discriminated by the `type` field.",
  "oneOf": [
    {
      "$ref": "#/$defs/UiToEngineMessage"
    },
    {
      "$ref": "#/$defs/EngineToUiMessage"
    }
  ],
  "$defs": {
    "SchemaVersion": {
      "title": "SchemaVersion",
      "description": "Bridge protocol version. Bump this whenever a breaking change is made to any message shape in this file; engine and UI both check it on stateChanged so a stale webview bundle fails loudly instead of silently desyncing. M2 wave 3 bumps 3 -> 4: the fixed 3-pedal chainOrder is replaced by 6 generic pedal slots (slot0Type..slot5P4 ParamIds; setSlotType/movePedal messages; rigState.slots replaces rigState.chainOrder).",
      "type": "integer",
      "minimum": 0,
      "const": 4
    },
    "ParamId": {
      "title": "ParamId",
      "description": "Identifiers for host-automatable parameters exposed by the engine's APVTS. M1 adds the gate/amp/cab rig params. M2 wave 3 adds 6 generic pedal slots (slot0..slot5), each a {Type, On, P1, P2, P3, P4} group (36 ids total), replacing the old fixed screamer/echoes/chamber ids. New params are added here first.",
      "type": "string",
      "enum": [
        "outputGain",
        "gateOn",
        "gateThresholdDb",
        "ampInputDb",
        "ampBassDb",
        "ampMidDb",
        "ampTrebleDb",
        "ampPresenceDb",
        "namNormalize",
        "cabOn",
        "cabLowCutHz",
        "cabHighCutHz",
        "slot0Type", "slot0On", "slot0P1", "slot0P2", "slot0P3", "slot0P4",
        "slot1Type", "slot1On", "slot1P1", "slot1P2", "slot1P3", "slot1P4",
        "slot2Type", "slot2On", "slot2P1", "slot2P2", "slot2P3", "slot2P4",
        "slot3Type", "slot3On", "slot3P1", "slot3P2", "slot3P3", "slot3P4",
        "slot4Type", "slot4On", "slot4P1", "slot4P2", "slot4P3", "slot4P4",
        "slot5Type", "slot5On", "slot5P1", "slot5P2", "slot5P3", "slot5P4"
      ]
    },
    "SlotIndex": {
      "title": "SlotIndex",
      "description": "A pedal slot index, 0..5. Slot index IS signal order (slot 0 is first in the signal path, right after the noise gate; slot 5 feeds the NAM amp) -- there is no separate chain-order concept.",
      "type": "integer",
      "minimum": 0,
      "maximum": 5
    },
    "PedalTypeId": {
      "title": "PedalTypeId",
      "description": "Identifies what pedal (if any) occupies a slot: 0 empty, 1 screamer, 2 echoes, 3 chamber, 4 squeeze, 5 wobble, 6 shiver. Matches the raw value of the engine's slot{N}Type AudioParameterChoice exactly.",
      "type": "integer",
      "enum": [0, 1, 2, 3, 4, 5, 6]
    },
    "SlotState": {
      "title": "SlotState",
      "description": "One pedal slot's current occupant + on/off, as reported on rigState.slots. The slot's four knob values (P1-4) are ordinary ParamIds (slot{N}P1..P4), not repeated here.",
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/$defs/PedalTypeId"
        },
        "on": {
          "type": "boolean"
        }
      },
      "required": [
        "type",
        "on"
      ],
      "additionalProperties": false
    },
    "NormalizedParamValue": {
      "title": "NormalizedParamValue",
      "description": "A parameter value normalized to the JUCE APVTS convention of 0..1, regardless of the parameter's real-world range/units (e.g. outputGain's underlying range is -60..+12 dB). The engine owns the mapping in both directions.",
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "DecibelValue": {
      "title": "DecibelValue",
      "description": "A signal level expressed in dBFS, as reported by a meter tap. Not clamped in the schema since engines may report -inf for digital silence; consumers should treat very negative / non-finite values as silence.",
      "type": "number"
    },
    "SetParam": {
      "title": "SetParam",
      "description": "UI -> engine. Set a parameter to a new normalized value, e.g. from a knob drag. The engine is the source of truth for the resulting real-world value and will echo it back via stateChanged.",
      "type": "object",
      "properties": {
        "type": {
          "const": "setParam"
        },
        "paramId": {
          "$ref": "#/$defs/ParamId"
        },
        "value": {
          "$ref": "#/$defs/NormalizedParamValue"
        }
      },
      "required": [
        "type",
        "paramId",
        "value"
      ],
      "additionalProperties": false
    },
    "LoadNamModel": {
      "title": "LoadNamModel",
      "description": "UI -> engine. Load a .nam model file. path must be an absolute path inside the managed Models library folder; the engine rejects anything else. The engine responds with a loadResult (kind: \"nam\") followed by a fresh rigState once the load completes (success or failure).",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadNamModel"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "additionalProperties": false
    },
    "LoadIr": {
      "title": "LoadIr",
      "description": "UI -> engine. Load a cab IR file (.wav/.aiff). path must be an absolute path inside the managed IRs library folder; the engine rejects anything else. The engine responds with a loadResult (kind: \"ir\") followed by a fresh rigState once the load completes (success or failure).",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadIr"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "additionalProperties": false
    },
    "RequestRigState": {
      "title": "RequestRigState",
      "description": "UI -> engine. Ask for a fresh rigState snapshot, e.g. when a library/overlay view mounts. The engine rescans the Models/IRs library folders before replying.",
      "type": "object",
      "properties": {
        "type": {
          "const": "requestRigState"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "SetSlotType": {
      "title": "SetSlotType",
      "description": "UI -> engine. Picks a new pedal (or empty, pedalType 0) for the given slot -- e.g. from the pedal palette. The engine resets that slot's on/P1-4 to their flat defaults (true/0.5/0.5/0.5/0.5), rebuilds the slot's DSP instance with a short click-free crossfade, and echoes a fresh rigState. slot/pedalType out of range is silently rejected -- no state change, no reply.",
      "type": "object",
      "properties": {
        "type": {
          "const": "setSlotType"
        },
        "slot": {
          "$ref": "#/$defs/SlotIndex"
        },
        "pedalType": {
          "$ref": "#/$defs/PedalTypeId"
        }
      },
      "required": [
        "type",
        "slot",
        "pedalType"
      ],
      "additionalProperties": false
    },
    "MovePedal": {
      "title": "MovePedal",
      "description": "UI -> engine. Moves the pedal at slot `from` to slot `to`, e.g. after a drag-reorder or keyboard swap gesture. Every slot strictly between `from` and `to` shifts by one to close the gap (a standard array move over all 6 slots, including empty ones) -- only the affected slots' PARAMETER VALUES (type/on/P1-4) move; host automation lanes stay slot-positional. from == to is a harmless no-op. from/to out of range is silently rejected -- no state change, no reply. On success the engine echoes a fresh rigState (including on a later requestRigState, and after DAW state restore).",
      "type": "object",
      "properties": {
        "type": {
          "const": "movePedal"
        },
        "from": {
          "$ref": "#/$defs/SlotIndex"
        },
        "to": {
          "$ref": "#/$defs/SlotIndex"
        }
      },
      "required": [
        "type",
        "from",
        "to"
      ],
      "additionalProperties": false
    },
    "LoadPreset": {
      "title": "LoadPreset",
      "description": "UI -> engine. Load a .sgpreset file. path must be an absolute path inside the managed Presets library folder; the engine rejects anything else. Restores model/IR/chain order/params via the same code path as a DAW session restore. The engine responds with a presetResult followed by a fresh presetsState once the load completes (success or failure).",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadPreset"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "additionalProperties": false
    },
    "SavePresetAs": {
      "title": "SavePresetAs",
      "description": "UI -> engine. Saves the current rig state as a new (or overwritten) preset named `name` and makes it the current preset. The engine sanitizes `name` into a safe filename; an empty/unsanitizable name is rejected. The engine responds with a presetResult (ok:false + a message on failure) followed by a fresh presetsState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "savePresetAs"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "name"
      ],
      "additionalProperties": false
    },
    "SaveCurrentPreset": {
      "title": "SaveCurrentPreset",
      "description": "UI -> engine. Overwrites the current preset with the current rig state. Fails (presetResult ok:false) if there is no current preset -- the UI should fall back to savePresetAs in that case. The engine responds with a presetResult followed by a fresh presetsState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "saveCurrentPreset"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "NextPreset": {
      "title": "NextPreset",
      "description": "UI -> engine. Loads the next preset (sorted by name) relative to the current one, wrapping around; loads the first preset if none is current. No-op if there are no presets. The engine responds with a fresh presetsState (no presetResult -- the UI has no inline failure surface for this control).",
      "type": "object",
      "properties": {
        "type": {
          "const": "nextPreset"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "PrevPreset": {
      "title": "PrevPreset",
      "description": "UI -> engine. Loads the previous preset (sorted by name) relative to the current one, wrapping around; loads the last preset if none is current. No-op if there are no presets. The engine responds with a fresh presetsState (no presetResult).",
      "type": "object",
      "properties": {
        "type": {
          "const": "prevPreset"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "RequestPresets": {
      "title": "RequestPresets",
      "description": "UI -> engine. Ask for a fresh presetsState snapshot, e.g. when the presets overlay mounts. The engine rescans the Presets folder before replying.",
      "type": "object",
      "properties": {
        "type": {
          "const": "requestPresets"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": false
    },
    "UiToEngineMessage": {
      "title": "UiToEngineMessage",
      "description": "Union of every message the React UI may send to the C++ engine.",
      "oneOf": [
        {
          "$ref": "#/$defs/SetParam"
        },
        {
          "$ref": "#/$defs/LoadNamModel"
        },
        {
          "$ref": "#/$defs/LoadIr"
        },
        {
          "$ref": "#/$defs/RequestRigState"
        },
        {
          "$ref": "#/$defs/SetSlotType"
        },
        {
          "$ref": "#/$defs/MovePedal"
        },
        {
          "$ref": "#/$defs/LoadPreset"
        },
        {
          "$ref": "#/$defs/SavePresetAs"
        },
        {
          "$ref": "#/$defs/SaveCurrentPreset"
        },
        {
          "$ref": "#/$defs/NextPreset"
        },
        {
          "$ref": "#/$defs/PrevPreset"
        },
        {
          "$ref": "#/$defs/RequestPresets"
        }
      ]
    },
    "MeterFrame": {
      "title": "MeterFrame",
      "description": "Engine -> UI. A single coalesced metering sample, pushed on a 30-60Hz timer off the audio thread (never per-block). M0 carries just the passthrough in/out peaks; more taps (per-block, tuner, etc.) are added as new fields in later milestones, not new message types.",
      "type": "object",
      "properties": {
        "type": {
          "const": "meterFrame"
        },
        "inPeakDb": {
          "$ref": "#/$defs/DecibelValue"
        },
        "outPeakDb": {
          "$ref": "#/$defs/DecibelValue"
        }
      },
      "required": [
        "type",
        "inPeakDb",
        "outPeakDb"
      ],
      "additionalProperties": false
    },
    "ParamValueMap": {
      "title": "ParamValueMap",
      "description": "Map of ParamId -> current normalized value, as sent in a full-state snapshot.",
      "type": "object",
      "propertyNames": {
        "$ref": "#/$defs/ParamId"
      },
      "additionalProperties": {
        "$ref": "#/$defs/NormalizedParamValue"
      }
    },
    "StateChanged": {
      "title": "StateChanged",
      "description": "Engine -> UI. Full-chain parameter snapshot, sent on connect and whenever the engine-side state changes for a reason the UI didn't itself just cause (host automation, preset load, etc). Carries schemaVersion so the UI can detect a protocol mismatch against a stale bundle.",
      "type": "object",
      "properties": {
        "type": {
          "const": "stateChanged"
        },
        "schemaVersion": {
          "$ref": "#/$defs/SchemaVersion"
        },
        "params": {
          "$ref": "#/$defs/ParamValueMap"
        }
      },
      "required": [
        "type",
        "schemaVersion",
        "params"
      ],
      "additionalProperties": false
    },
    "LibraryEntry": {
      "title": "LibraryEntry",
      "description": "One file in the managed Models or IRs library folder.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "path"
      ],
      "additionalProperties": false
    },
    "RigLibrary": {
      "title": "RigLibrary",
      "description": "Contents of the managed library folders (Documents/Simple Guitar/Models, .../IRs), rescanned each time a rigState is produced.",
      "type": "object",
      "properties": {
        "models": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LibraryEntry"
          }
        },
        "irs": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LibraryEntry"
          }
        }
      },
      "required": [
        "models",
        "irs"
      ],
      "additionalProperties": false
    },
    "RigState": {
      "title": "RigState",
      "description": "Engine -> UI. Full rig snapshot: currently loaded NAM model / IR (if any), the 6 pedal slots' type + on/off, and the managed library contents. Sent on page load, after any loadNamModel/loadIr/setSlotType/movePedal completes, and in reply to requestRigState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "rigState"
        },
        "schemaVersion": {
          "$ref": "#/$defs/SchemaVersion"
        },
        "namModelName": {
          "type": [
            "string",
            "null"
          ]
        },
        "namModelSampleRate": {
          "type": "number"
        },
        "irName": {
          "type": [
            "string",
            "null"
          ]
        },
        "slots": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/SlotState"
          },
          "minItems": 6,
          "maxItems": 6
        },
        "library": {
          "$ref": "#/$defs/RigLibrary"
        }
      },
      "required": [
        "type",
        "schemaVersion",
        "namModelName",
        "namModelSampleRate",
        "irName",
        "slots",
        "library"
      ],
      "additionalProperties": false
    },
    "LoadResultKind": {
      "title": "LoadResultKind",
      "description": "Which loader a loadResult reports on.",
      "type": "string",
      "enum": [
        "nam",
        "ir"
      ]
    },
    "LoadResult": {
      "title": "LoadResult",
      "description": "Engine -> UI. Outcome of a loadNamModel or loadIr request. message is a human-readable success/failure description (e.g. the resolved model name, or an error). Always followed by a rigState with the up-to-date snapshot.",
      "type": "object",
      "properties": {
        "type": {
          "const": "loadResult"
        },
        "kind": {
          "$ref": "#/$defs/LoadResultKind"
        },
        "ok": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "kind",
        "ok",
        "message"
      ],
      "additionalProperties": false
    },
    "PresetsState": {
      "title": "PresetsState",
      "description": "Engine -> UI. Full preset snapshot: the current preset (if any), whether the rig has changed since the last save/load, and the managed Presets library contents (flat, sorted by name). Sent on page load, after any loadPreset/savePresetAs/saveCurrentPreset/nextPreset/prevPreset resolves, in reply to requestPresets, and whenever `dirty` flips (throttled to ~4 emissions/second max so a fast knob drag doesn't flood the bridge).",
      "type": "object",
      "properties": {
        "type": {
          "const": "presetsState"
        },
        "schemaVersion": {
          "$ref": "#/$defs/SchemaVersion"
        },
        "current": {
          "oneOf": [
            {
              "$ref": "#/$defs/LibraryEntry"
            },
            {
              "type": "null"
            }
          ]
        },
        "dirty": {
          "type": "boolean"
        },
        "presets": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/LibraryEntry"
          }
        }
      },
      "required": [
        "type",
        "schemaVersion",
        "current",
        "dirty",
        "presets"
      ],
      "additionalProperties": false
    },
    "PresetResult": {
      "title": "PresetResult",
      "description": "Engine -> UI. Outcome of a loadPreset, savePresetAs, or saveCurrentPreset request (mirrors LoadResult's nam/ir pairing with rigState). message is human-readable (e.g. an error like \"name required\", or a short confirmation). Always followed by a fresh presetsState.",
      "type": "object",
      "properties": {
        "type": {
          "const": "presetResult"
        },
        "ok": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "ok",
        "message"
      ],
      "additionalProperties": false
    },
    "EngineToUiMessage": {
      "title": "EngineToUiMessage",
      "description": "Union of every message the C++ engine may send to the React UI.",
      "oneOf": [
        {
          "$ref": "#/$defs/MeterFrame"
        },
        {
          "$ref": "#/$defs/StateChanged"
        },
        {
          "$ref": "#/$defs/RigState"
        },
        {
          "$ref": "#/$defs/LoadResult"
        },
        {
          "$ref": "#/$defs/PresetsState"
        },
        {
          "$ref": "#/$defs/PresetResult"
        }
      ]
    }
  }
}
